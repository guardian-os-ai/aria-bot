# python-engine requirements
# Install with: pip install -r requirements.txt

# Core
chromadb>=0.4.0
requests>=2.28.0
rank-bm25>=0.2.2

# Local LLM (optional — only needed if using offline AI)
# Uncomment to enable local Phi-3 / Mistral inference:
# llama-cpp-python>=0.2.0
